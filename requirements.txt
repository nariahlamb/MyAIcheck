Flask
gunicorn
asyncio
typing_extensions==4.13.2
# The following dependencies were removed as they don't appear to be used by the main Flask application:
# aiohappyeyeballs==2.6.1
# aiohttp==3.11.18
# 其余依赖如csv、io、json、os、sys、typing为Python标准库，无需额外声明
